# 开发环境配置：通过 Vite 同源代理转发到本机后端。
# 不要设置 VITE_API_BASE_URL 或 VITE_WS_BASE_URL：这样局域网浏览器访问
# http://SERVER_IP:3000 时，请求仍会由该 Vite 服务转发，而不会错误指向浏览器自己的 localhost。
# VITE_API_BASE_URL=
# VITE_WS_BASE_URL=
VITE_ENV=development